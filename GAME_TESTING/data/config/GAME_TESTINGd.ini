; GAME_TESTING - Basic template config file

; Loads regular config data
@GAME_TESTING.ini@

; Overrides some values for debug
[Display]
Title           = GAME_TESTING (Debug)

[Render]
ShowFPS         = true

[Resource]
WatchList       = Config # Texture # Sound

[HeroGraphic]
Texture         = Jorge.png
TextureOrgin    = (0,0,0)
TextureSize     = (32,32,0)
